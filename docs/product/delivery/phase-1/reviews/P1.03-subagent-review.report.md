I have all the information needed. Here is the adversarial review report.

---

**Invariant results**

**Invariant 1 — prev/next links not rendered as `<a>` on first/last lesson:** *Held for the normal case, but broken under the findIndex -1 edge case (see Surface 1 and F1).* For a valid `currentIndex` (0-based), `prev` is null at index 0 and `next` is null at the last index. The conditional in `LessonLayout.astro` lines 18–21 is correct when `currentIndex ≥ 0`.

**Invariant 2 — breadcrumb hierarchy correctly reflects current page:** *Held.* Standalone lesson: `[Home, Subject(link), Title(span)]`. Unit-grouped lesson: `[Home, Subject(link), Unit(link→subject), Title(span)]`. Subject landing: `[Home, Subject(span)]`. All three patterns match the ticket outcome exactly.

**Invariant 3 — every home-feed lesson also appears on its subject landing page:** *Held.* Both `index.astro` and `[subject]/index.astro` call `getCollection('lessons')` and filter by `data.subject` using an identical equality predicate. There is no secondary key or transformation that could cause a lesson to appear on the home feed but be absent from its subject listing.

**Surface results**

**Surface 1 — `findIndex` returning -1:** *Probed — correctness gap confirmed.* `LessonLayout.astro` line 20 computes `next` as `currentIndex < allLessons.length - 1 ? allLessons[currentIndex + 1] : null`. When `currentIndex = -1`, the condition `-1 < allLessons.length - 1` is true for any non-empty collection, so `allLessons[-1 + 1]` = `allLessons[0]` is returned and rendered as a live `<a>` link — the first lesson incorrectly appears as "next" on every lesson page. This breaks Invariant 1. See F1.

**Surface 2 — `Breadcrumb.astro` middle item without `href`:** *Probed — no live breakage; silently degrades.* The condition `i < items.length - 1 && item.href` renders a non-last item missing `href` as a `<span>` instead of a link. All current callers (`LessonLayout.astro` and `[subject]/index.astro`) always supply `href` for every non-last item, so no breadcrumb is silently broken today. See A2.

**Surface 3 — unit breadcrumb `href: /${subject}`:** *Probed — clean.* The unit item in `LessonLayout.astro` line 33 links to `/${subject}`, which is the subject landing page generated by `[subject]/index.astro`. That page always exists for any lesson that reaches `LessonLayout`. The ticket rationale explicitly defers dedicated unit pages, so this is intentional and no broken link exists.

**Surface 4 — `[subject]/index.astro` getStaticPaths completeness:** *Probed — clean.* `getStaticPaths` derives subject slugs via `new Set(entries.map(e => e.data.subject))`, then page-level code filters with `l.data.subject === subject` — the same string from the same collection field. Every lesson on the home feed (`getCollection('lessons')` unfiltered) is covered by exactly one subject page, satisfying Invariant 3.

**Surface 5 — URL construction `/${lesson.slug}`:** *Probed — clean.* For `programming-fundamentals/what-is-a-program` (2-segment slug), `/${lesson.slug}` = `/programming-fundamentals/what-is-a-program`, which resolves to `[subject]/[lesson].astro` (params `{subject:"programming-fundamentals", lesson:"what-is-a-program"}`). For `programming-fundamentals/unit-1/variables-and-io` (3-segment slug), the URL resolves to `[subject]/[unit]/[lesson].astro` (params `{subject, unit:"unit-1", lesson:"variables-and-io"}`). Route file filters (`slug.split('/').length === 2` vs `=== 3`) correctly gate each entry to its matching route. All constructed hrefs match the route file URL patterns.

**Diff-derived surface 1 — Output stability across schema-version drift:** N/A — no persisted JSON artifacts or CLI output shapes changed in this diff.

**Diff-derived surface 2 — CLI flag/arg symmetry:** N/A — no CLI commands or argument parsers introduced or modified.

**Diff-derived surface 3 — Error-class breadth in catch blocks:** N/A — no try/catch blocks in any changed file.

**Diff-derived surface 4 — Defensive layering at module boundaries:** *Probed — safe for current mode.* `const subject = Astro.params.subject as string` in `[subject]/index.astro` line 13 casts away `string | undefined`. At static generation time, `Astro.params.subject` is always a string derived from `getStaticPaths`, so the cast is sound. See A3 for the SSR-mode latent gap.

**Diff-derived surface 5 — Cross-file atomicity windows:** N/A — all writes are Astro static-build outputs; no multi-step state mutations occur.

**Diff-derived surface 6 — Test-contract strength:** N/A — `Red: skip` per ticket spec; visual navigation on Vercel preview is the stated verification mechanism.

**Diff-derived surface 7 — Doc-vs-code drift in the ticket Rationale:** *Probed — drift found.* The ticket's Refactor section states: "Ensure the home page lesson query is reusable for the subject page (shared utility or collection helper)." The diff does not implement a shared utility. `index.astro` and `[subject]/index.astro` both independently call `getCollection('lessons')`, sort by `order`, and define an identical `formatSubject` function. `LessonLayout.astro` defines the same slug-to-title-case logic under the name `formatLabel`. Three copies of the same transform, no shared helper. See A1.

**Actionable findings**

**F1 — `src/layouts/LessonLayout.astro` line 20 — `next` unguarded when `findIndex` returns -1**

When `allLessons.findIndex((l) => l.slug === entry.slug)` returns -1 (e.g., due to slug encoding or normalization differences between two `getCollection` calls — plausible on Windows where path separators differ), `next` evaluates to `allLessons[0]` because `-1 < allLessons.length - 1` is true. The first lesson is then rendered as an incorrect `<a>` link on every lesson page, breaking Invariant 1.

Applies under finding-discipline clause 2: correctness gap demonstrable from the code.

Concrete fix — change line 19–21:
```diff
-const next =
-  currentIndex < allLessons.length - 1 ? allLessons[currentIndex + 1] : null
+const next =
+  currentIndex >= 0 && currentIndex < allLessons.length - 1
+    ? allLessons[currentIndex + 1]
+    : null
```

**Advisory Observations**

**A1 — Refactor contract not fulfilled (doc-vs-code drift):** The ticket's Refactor section requires "Ensure the home page lesson query is reusable for the subject page (shared utility or collection helper)." The diff duplicates `getCollection('lessons')`, the sort by `order`, and the slug-to-title-case transform in three separate files (`index.astro`, `[subject]/index.astro`, and `LessonLayout.astro` as `formatLabel` vs `formatSubject`). No shared utility or collection helper was created. The Outcome section is satisfied — behavior is correct — but the Refactor clause was left unimplemented. Primary agent should decide whether to extract a shared helper or update the Refactor note to mark this as deferred.

**A2 — `Breadcrumb.astro` silent span degradation for non-last items missing `href`:** The condition `i < items.length - 1 && item.href` silently renders a non-last item with `href: undefined` as a plain `<span>`. Current callers always supply `href` for non-last items, so no breadcrumb is broken today. The TypeScript interface `{ label: string; href?: string }` does not enforce this constraint, so a future caller constructing a mid-hierarchy item without `href` would silently produce an unlinked breadcrumb segment rather than a type error.

**A3 — `as string` cast in `[subject]/index.astro` line 13:** `Astro.params.subject as string` is safe in static generation (param always defined by `getStaticPaths`) but would be unsafe if the site were switched to SSR mode. No current risk; documents a latent type safety gap if output mode ever changes.

**Runner termination**
runnerStatus: completed
terminatedReason: All invariants probed, all surfaces evaluated, one actionable finding and three advisory observations reported.
