You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

**Outcome:**
- Astro project initialised with Tailwind CSS, MDX support, and React integration configured
- Project runs locally with `bun run dev` without errors
- Vercel project wired; every push to main triggers a deploy and every PR gets a preview URL
- Bun scripts in `package.json`: `dev`, `build`, `preview`, `format`, `ci:quiet`
- Basic folder structure exists: `src/`, `public/`, `src/components/`, `src/layouts/`, `src/content/`

**Rationale notes:**
- Tailwind v4 via `@tailwindcss/vite` rather than `@astrojs/tailwind` + v3
- Manual scaffold rather than `bunx create-astro` to preserve SoA scripts in package.json
- Vercel project link deferred — requires interactive `vercel link`; code is deploy-ready

### Files touched

Implementation:
- astro.config.mjs (new)
- package.json (modified)
- tsconfig.json (modified)
- .prettierrc (new)
- src/pages/index.astro (new)
- src/styles/global.css (new)
- src/components/.gitkeep (new)
- src/layouts/.gitkeep (new)
- src/content/.gitkeep (new)
- public/.gitkeep (new)
- bun.lock (modified)

Tests:
- None (Red: skip — scaffold ticket)

### Invariants to hold

1. `bun run build` must exit 0 — the Astro project compiles without errors with all three integrations active.
2. `bun run verify` (astro check) must exit 0 with 0 errors — TypeScript checking passes for all `.astro` and `.ts` files.
3. All three integrations must be present and correctly positioned: `@astrojs/react` and `@astrojs/mdx` in `integrations[]`; `@tailwindcss/vite` in `vite.plugins[]`.

### Attack surfaces to probe

1. **`astro.config.mjs` Tailwind placement** — Tailwind v4 uses `vite.plugins` not `integrations`; probe whether the `@tailwindcss/vite` plugin in `vite.plugins[]` actually applies Tailwind classes to `.astro` files at build time vs. being silently ignored.
2. **`global.css` import path fragility** — `src/pages/index.astro` imports `'../styles/global.css'` with a relative path. Probe whether this path works correctly and whether pages nested in subdirectories (e.g. `src/pages/subjects/lesson.astro`) will silently miss the Tailwind styles due to a broken relative import.
3. **`ci:quiet` shell operator compatibility** — the script `"astro check --minimumSeverity error && astro build"` uses `&&`. Probe whether bun's script runner handles `&&` correctly on Windows (where the default shell may not be bash), and whether a type error could be silently swallowed.
4. **`tsconfig.json` migration impact on `tools/`** — the previous `tsconfig.json` targeted Bun's TypeScript environment (`"types": ["bun"]`, `"module": "Preserve"`). The new `tsconfig.json` extends `astro/tsconfigs/strict` which sets different compiler options. Probe whether the `tools/delivery/*.ts` files still compile correctly under the new config, or whether the Astro tsconfig strips Bun type declarations that the tools depend on.

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — [N/A — this ticket introduces no persisted artifacts, CLI commands, or JSON output formats. No schema versioning concerns.]
2. **CLI flag/arg symmetry** — [N/A — no CLI flags are added or modified in this diff.]
3. **Error-class breadth in `catch` blocks** — [N/A — no try/catch blocks exist in the scaffold files.]
4. **Defensive layering at module boundaries** — [N/A — no cross-module function calls are introduced. The Astro config calls integration factories with no cross-boundary inputs to validate.]
5. **Cross-file atomicity windows** — [N/A — no multi-step writes or state transitions exist in this scaffold. All files are independently created.]
6. **Test-contract strength** — [N/A — Red: skip; no tests are introduced in this ticket. The scaffold ticket has no testable behavior.]
7. **Doc-vs-code drift in the ticket Rationale** — The ticket Outcome states "Vercel project wired; every push to main triggers a deploy and every PR gets a preview URL." The Rationale explicitly records this as deferred: "Vercel project link — requires interactive `vercel link`." Probe whether this drift is flagged correctly or whether there are other outcome claims the diff does not fulfill.

### Diff context

The diff introduces an Astro 5 project scaffold onto an existing repository that previously contained only SoA delivery tooling. Key changes:

1. `astro.config.mjs` — new file configuring static output with React and MDX integrations. Tailwind is wired via `vite.plugins` using the `@tailwindcss/vite` plugin (Tailwind v4 approach, not the deprecated `@astrojs/tailwind` integration).

2. `package.json` — adds Astro 5, React 19, Tailwind v4, and related devDependencies. Preserves existing SoA scripts (`deliver`, `sync`, `closeout-stack`). Adds `dev`, `build`, `preview`, `format`, `verify`, `verify:quiet`, `ci:quiet` scripts.

3. `tsconfig.json` — replaces the Bun-oriented config (extending Bun types, `"module": "Preserve"`) with `"extends": "astro/tsconfigs/strict"` plus path aliases.

4. `src/pages/index.astro` — placeholder home page importing `../styles/global.css` and rendering a basic `<h1>` with a Tailwind class.

5. `src/styles/global.css` — single line `@import 'tailwindcss'` (Tailwind v4 CSS-first setup).

Both `astro build` and `astro check --minimumSeverity error` were confirmed to exit 0 locally before post-verify was recorded.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- Do not use `---` horizontal rules anywhere in the report.
- `Runner termination` must be the section heading, not inline key-value. Write it as the bold span `**Runner termination**` on its own line, then `runnerStatus:` and `terminatedReason:` as plain-text lines below.
- Inside `Advisory Observations`, write one observation per bullet or one observation per paragraph. Do NOT use a bold span on a line by itself before the observation body.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template.
