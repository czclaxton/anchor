I have all the information I need. Here is the formal adversarial review report.

---

**Invariant results**

1. `bun run build` exits 0 — **held**. The `astro.config.mjs` wires all three integrations correctly. The rationale records that `astro build` was confirmed to exit 0 locally before post-verify was recorded. No structural issue in the config could cause a build failure.

2. `bun run verify` exits 0 with 0 errors — **held**. `astro check --minimumSeverity error` was confirmed locally per the rationale. The new `tsconfig.json` `include` field (`["src", "*.mjs", "*.ts"]`) explicitly limits the scope to the Astro source tree, so the only checked files are `.astro` and `.ts` files under `src/` plus root-level `.ts`/`.mjs` files. Nothing in that scope introduces a type error.

3. All three integrations present and correctly positioned — **held**. `astro.config.mjs` line 8: `integrations: [react(), mdx()]` — both `@astrojs/react` and `@astrojs/mdx` are in `integrations[]`. Line 10: `plugins: [tailwindcss()]` — `@tailwindcss/vite` is in `vite.plugins[]`. All three positions are correct for Tailwind v4.

**Surface results**

1. **`astro.config.mjs` Tailwind placement** — probed. `tailwindcss()` is correctly in `vite.plugins[]`. In Tailwind v4, the `@tailwindcss/vite` plugin hooks into Vite's CSS transform pipeline and auto-scans project files (including `.astro` files) to emit utility classes. The `@import 'tailwindcss'` directive in `src/styles/global.css` activates the plugin. Classes used in `src/pages/index.astro` (`text-4xl`, `font-bold`, `text-gray-600`) will be detected during the scan. No silent-ignore path found.

2. **`global.css` import path fragility** — probed. The import `'../styles/global.css'` in `src/pages/index.astro` resolves correctly for files directly in `src/pages/`. A page at `src/pages/subjects/lesson.astro` would need `'../../styles/global.css'` — a different relative path that Astro will not warn about if wrong (the import simply 404s silently in SSG output). Tailwind v4 still scans and emits utility classes from nested pages regardless, so class output is not affected; the risk is that nested pages do not receive the global CSS cascade if they omit or misspell the import. No finding raised because no nested pages exist yet and the ticket does not require them. Flagged in Advisory Observations.

3. **`ci:quiet` shell operator compatibility on Windows** — probed. Bun ships a built-in cross-platform shell (bun-shell) since v1.0 that supports `&&`, `||`, and pipes on Windows without relying on `cmd.exe` or PowerShell. The `&&` in `"astro check --minimumSeverity error && astro build"` will be handled correctly by bun's script runner. A type error in `astro check` will exit non-zero and prevent `astro build` from running as expected. One naming anomaly noted in Advisory Observations.

4. **`tsconfig.json` migration impact on `tools/`** — probed. The pre-scaffold `tsconfig.json` (verified via `git show HEAD~3:tsconfig.json`) had no `include` field, which in TypeScript means all `.ts`/`.tsx` files in the project tree are included by default — including `tools/delivery/*.ts`. The new config specifies `"include": ["src", "*.mjs", "*.ts"]`. The `*.ts` glob matches only root-level `.ts` files; `tools/delivery/*.ts` is not a root-level file and is not covered by `src`. The Astro base config (`node_modules/astro/tsconfigs/base.json`) has its own `include`, but child `include` arrays fully replace parent `include` arrays in TypeScript — so the project's narrow include wins. Confirmed: `tools/delivery/platform.ts:109` uses `Bun.spawnSync()`, which requires `@types/bun`. Under the Astro tsconfig, if these files were in scope, they would fail because `astro/tsconfigs/strict` does not declare `@types/bun`. They are silently excluded from type checking instead. `@types/bun` remains in `devDependencies` so Bun runtime execution is unaffected. No invariant is broken, but static type coverage over the delivery tools is now zero. Flagged in Advisory Observations.

5. **Output stability across schema-version drift** — N/A: this ticket introduces no persisted artifacts, CLI commands, or JSON output formats.

6. **CLI flag/arg symmetry** — N/A: no CLI flags added or modified.

7. **Error-class breadth in `catch` blocks** — N/A: no try/catch blocks in the scaffold files.

8. **Defensive layering at module boundaries** — N/A: no cross-module function calls introduced.

9. **Cross-file atomicity windows** — N/A: no multi-step writes or state transitions.

10. **Test-contract strength** — N/A: Red: skip; no tests introduced.

11. **Doc-vs-code drift in the ticket Rationale** — probed. Three distinct drift sites found in `docs/product/delivery/phase-1/ticket-01-project-scaffold.md`:

    - **Outcome line 11**: "Vercel project wired; every push to main triggers a deploy and every PR gets a preview URL." The diff does not wire Vercel. No `vercel.json`, no `vercel link`, no deploy URL. The Rationale (line 47) correctly records this as deferred, but the Outcome line itself is unfulfilled and creates a false impression of completeness.

    - **Green section lines 22–26**: Describes `bunx create astro@latest`, `astro add @astrojs/tailwind`, and creating a Vercel project — none of which match the actual implementation. The real approach (manual scaffold, `@tailwindcss/vite`, no Vercel link) is explained only in the Rationale append block.

    - **Review Focus lines 36–38**: "Vercel deploy succeeds on the preview URL — paste the URL in the PR description." No URL is present and no deploy occurred.

**Actionable findings**

None.

**Advisory Observations**

The `tsconfig.json` `include` field change silently drops `tools/delivery/*.ts` from TypeScript coverage. Before the scaffold commit, the absence of an `include` field meant TypeScript (and `astro check` via the language server) covered the full project tree. Now the explicit `["src", "*.mjs", "*.ts"]` excludes all files under `tools/`. Because `tools/delivery/platform.ts` uses `Bun.spawnSync()` (line 109) — a Bun global that requires `@types/bun` — these files would fail if re-included under the Astro tsconfig. The practical effect is that the delivery tools are now type-checked by nothing in CI. The `bun run verify` gate will not catch regressions in `tools/`. The primary agent may want to evaluate whether to add a separate `bun run tsc --project tsconfig.tools.json` gate with the original Bun-oriented config, or accept the coverage gap as a deliberate trade-off.

`verify` and `verify:quiet` in `package.json` (lines 10–11) are character-for-character identical: both run `astro check --minimumSeverity error` with no output suppression. There is no quiet behavior. `ci:quiet` similarly produces stdout/stderr. The `:quiet` naming in scripts implies silenced success output (common in CI contexts) but the flag does not exist in `astro check`. If the intent was a truly quiet gate, `astro check --minimumSeverity error 2>&1 | grep -v "^$"` or a wrapper script is needed. This is a naming inconsistency, not a functional failure.

Pages created in `src/pages/` subdirectories (e.g., `src/pages/subjects/lesson.astro`) will need to adjust the relative `global.css` import path — `'../styles/global.css'` becomes `'../../styles/global.css'` one level deeper. Astro gives no warning if the import silently resolves to nothing in SSG mode. This is fragile as the lesson-content tickets (P1.06–P1.11) add pages under subject subdirectories. A layout-based import of `global.css` would make this robust, but no layout is introduced until ticket P1.03. The primary agent should note this risk when implementing P1.06.

The ticket Outcome, Green section, and Review Focus in `ticket-01-project-scaffold.md` all describe Vercel wiring that was not delivered. The Rationale correctly marks this deferred. The three stale sections (Outcome line 11, Green lines 25–27, Review Focus lines 36–38) will mislead reviewers checking completeness against the ticket unless they read to the Rationale append. No code change is needed, but the primary agent may want to strike through or annotate the stale Outcome bullet and Review Focus clause so the ticket accurately reflects what was shipped.

**Runner termination**
`runnerStatus`: completed
`terminatedReason`: All invariants probed and all attack surfaces covered; review finished without tool denial or rate limiting.
