You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

**Outcome:**
- `src/content/lessons/programming-fundamentals/what-is-a-program.mdx` exists and is complete (intro page, chef analogy)
- `src/content/lessons/programming-fundamentals/unit-1/variables.mdx` exists and is complete (Variables lesson, novel placeholder analogy)
- All lesson layout slots are filled: hook, analogy, explanation, interactive (placeholder), summary
- `src/content/lessons/programming-fundamentals/unit-1/variables-and-io.mdx` placeholder is removed
- Astro build generates `/programming-fundamentals/what-is-a-program/` and `/programming-fundamentals/unit-1/variables/` routes

**Rationale notes:**
- Curriculum restructured: "What is a Program?" became a brief standalone intro page (no unit); Variables became the first real lesson (unit-1, order 2)
- `variables-and-io.mdx` placeholder (created in P1.02, order 2) deleted and replaced with `variables.mdx` (order 2) — route changes from `/programming-fundamentals/unit-1/variables-and-io` to `/programming-fundamentals/unit-1/variables`
- Interactive slot left as a JSX comment placeholder wired to P1.07

### Files touched

Implementation:
src/content/lessons/programming-fundamentals/what-is-a-program.mdx
src/content/lessons/programming-fundamentals/unit-1/variables.mdx
src/content/lessons/programming-fundamentals/unit-1/variables-and-io.mdx (deleted)
docs/product/delivery/phase-1/ticket-06-lesson-1-content.md
.agents/delivery/phase-1/state.json
.agents/delivery/phase-1/handoffs/p1-05-handoff.md
.agents/delivery/phase-1/handoffs/p1-06-handoff.md

Tests:
None (Red: skip — content quality is human-reviewed)

### Invariants to hold

1. Both `variables.mdx` and `what-is-a-program.mdx` must pass Astro's content collection schema validation — all required fields (`title`, `subject`, `order`, `analogy`, `hook`) present and correctly typed; `unit` present in `variables.mdx` as a non-empty string.
2. The Astro static build must produce `/programming-fundamentals/unit-1/variables/index.html` and `/programming-fundamentals/what-is-a-program/index.html` — and must NOT produce `/programming-fundamentals/unit-1/variables-and-io/index.html`.
3. Every named slot used in both MDX files (`hook`, `analogy`, `explanation`, `interactive`, `summary`) must exactly match the slot names consumed by `LessonLayout.astro` — a mismatched slot name silently falls through to the placeholder, leaving a blank section with no build error.

### Attack surfaces to probe

1. **`variables.mdx` frontmatter — schema field completeness and type correctness:** Does every required field exist and match the Zod schema in `src/content/config.ts`? Specifically: is `unit` present and non-empty (schema requires `z.string().min(1)`)? Is `order: 2` a number (not a string)? Are `analogy` and `hook` non-empty strings?

2. **Slot name alignment between `variables.mdx` / `what-is-a-program.mdx` and `LessonLayout.astro`:** The MDX uses `<Fragment slot="explanation">` and `<Fragment slot="interactive">`. Does `LessonLayout.astro` declare matching `<slot name="explanation">` and `<slot name="interactive">` elements? A typo in either file renders the slot silently empty.

3. **Prev/next navigation in `LessonLayout.astro` across unit/no-unit lesson mix:** `LessonLayout` queries `getCollection('lessons')`, sorts all by `order`, and builds prev/next links. With `what-is-a-program.mdx` at `order: 1` (no unit) and `variables.mdx` at `order: 2` (unit-1), does the sort produce the correct adjacency? Specifically: does the prev link on the Variables lesson correctly point to "What is a Program?"?

4. **Dead route from `variables-and-io.mdx` deletion:** The subject index at `/programming-fundamentals/index.astro` or the home page may enumerate or link lessons from the content collection. Does removing `variables-and-io.mdx` leave a 404-able hard-coded link anywhere? Check `src/pages/[subject]/index.astro` and `src/pages/index.astro` for static lesson href references.

5. **`what-is-a-program.mdx` missing `unit` field — breadcrumb rendering:** `LessonLayout.astro` conditionally includes the unit breadcrumb: `...(unit ? [{ label: formatSlug(unit), ... }] : [])`. Confirm the ternary correctly skips the unit breadcrumb when `unit` is undefined (no-unit lesson), and does not produce a broken `formatSlug(undefined)` call.

6. **Interactive slot JSX comment — render safety:** Both MDX files use `{/* ... */}` comments inside `<Fragment slot="interactive">`. Confirm JSX comments inside MDX slot fragments do not emit visible text or cause a hydration error when rendered by Astro.

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — [N/A — no persisted JSON artifacts changed in this diff; delivery state.json update is an orchestrator internal and does not affect consumer schema]

2. **CLI flag/arg symmetry** — [N/A — no CLI or orchestrator code changed in this diff]

3. **Error-class breadth in `catch` blocks** — [N/A — no TypeScript logic added; MDX and markdown content only]

4. **Defensive layering at module boundaries** — Probe the Astro content collection boundary: does `src/content/config.ts` Zod schema correctly enforce `unit: z.string().min(1).optional()`? If `unit` were accidentally set to an empty string `""` in a future lesson, would the schema catch it? Confirm the `.min(1)` constraint is present and the build would fail on `unit: ""`.

5. **Cross-file atomicity windows** — [N/A — this is a content commit with no multi-step write sequences; old file deleted and new file added atomically in one commit]

6. **Test-contract strength** — [N/A — Red: skip; no automated tests for content; Astro's `astro check` is the validation gate and CI confirms it passed with 0 errors]

7. **Doc-vs-code drift in the ticket Rationale** — Probe whether the ticket Rationale claims match the diff: (a) does the rationale's stated route change (`/variables-and-io` → `/variables`) match what the deletion+addition in the diff produces? (b) does the rationale's claim that "interactive slot wired to P1.07 (isometric park scene, pending)" match the JSX comment in the MDX? (c) does the "What is a Program?" intro page description in the design session doc match the content actually written in `what-is-a-program.mdx`?

### Diff context

**what-is-a-program.mdx:** Replaced placeholder content with full lesson. Frontmatter: `title: "What is a Program?"`, `subject: programming-fundamentals`, `order: 1`, no `unit` field. Slots filled: hook (paralyzed chef opening), analogy (chef + volunteer crew metaphor), explanation (literal instruction following → defines "program" and "bug"), interactive (JSX comment placeholder), summary (volunteers + "in next lesson" teaser).

**variables.mdx (new):** `title: Variables`, `subject: programming-fundamentals`, `unit: unit-1`, `order: 2`. Slots: hook (propagation question), analogy (author uses `hero_name` placeholder in manuscript), explanation (Python assignment syntax, three type examples, reference to interactive), interactive (JSX comment placeholder for P1.07), summary (placeholder → variable as propagation mechanism).

**variables-and-io.mdx (deleted):** Had `title: Variables and I/O`, `order: 2`, `unit: unit-1`. Placeholder body only. Route `/programming-fundamentals/unit-1/variables-and-io` no longer exists.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
