```
You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

**Outcome:**
- Each lesson page shows an "Anchored!" button at the bottom
- Clicking "Anchored!" triggers an anchor drop/settle CSS animation and marks the lesson complete in localStorage
- A progress bar appears on lesson pages (and the subject landing page) showing X of N lessons completed in the current unit/subject
- The progress bar uses an anchor motif — chain links filling in, or an anchor icon at the completion end
- Progress persists across page refreshes and browser sessions (localStorage)
- Completing the same lesson twice does not create duplicate entries or break the progress count
- Vitest tests pass for the localStorage read/write logic

**Rationale:**
- Plain TS utility functions (`markComplete`, `getProgress`, `isComplete`) keep the localStorage logic testable in Vitest without needing jsdom or React Testing Library. React components are thin client islands that call the utility on mount and click.
- `entry.slug` (the full Astro content collection slug) is used as the per-lesson unique identifier in localStorage. The subject slug namespaces the key as `anchor:progress:<subjectSlug>`.
- Animation: `dropping` state triggers `animate-bounce` on the ⚓ span for 600ms on click, then clears.

### Files touched

Implementation:
- src/lib/progress.ts (new) — markComplete, getProgress, isComplete utilities
- src/components/AnchoredButton.tsx (new) — React island for the completion button
- src/components/ProgressBar.tsx (new) — React island for the anchor-motif progress bar
- src/layouts/LessonLayout.astro (modified) — wires in AnchoredButton and ProgressBar islands
- src/pages/[subject]/index.astro (modified) — wires in ProgressBar island

Tests:
- src/lib/progress.test.ts (new) — 3 Vitest tests for localStorage read/write/dedup logic

### Invariants to hold

1. `markComplete(lessonSlug, subjectSlug)` must be idempotent: calling it N times with the same `lessonSlug` and `subjectSlug` stores exactly one entry in the JSON array at `anchor:progress:<subjectSlug>` — never more, regardless of how many times it is called.
2. `getProgress(subjectSlug, totalLessons)` must return `{ completed: C, total: totalLessons }` where `C` is the count of unique lesson slugs currently stored for that subject key — not zero, not over-counted.
3. The `AnchoredButton` component must call `markComplete` at most once per lesson per browser session: after the first click, the button is disabled (`disabled={anchored}`) and the `handleClick` guard (`if (anchored) return`) prevents re-invocation.

### Attack surfaces to probe

1. `markComplete` in `progress.ts` — corrupt localStorage data: if `localStorage.getItem` returns an invalid JSON string (e.g. from a prior schema, a browser extension, or manual devtools injection), `JSON.parse` throws. There is no try/catch. Does this break silently, leaving progress tracking dead with an uncaught exception?
2. `isComplete` in `progress.ts` — unchecked cast: `JSON.parse(existing) as string[]` is a TypeScript assertion with no runtime guard. If the stored value is a JSON object or primitive (not an array), `.includes(lessonSlug)` will throw or return undefined. Does this path guard correctly?
3. `AnchoredButton.handleClick` — rapid-click race before setTimeout fires: the `if (anchored) return` guard uses React state (`anchored`), which is asynchronous. The `dropping` flag is set to `true` synchronously, but `anchored` is still `false` until the 600ms timer fires. Two rapid clicks (< 600ms apart) both pass the `if (anchored) return` guard and both call `markComplete`. Does the deduplication in `markComplete` protect against this, or can the observable button state diverge?
4. `ProgressBar` stale-read-on-mount — the component reads localStorage once in `useEffect` on mount. If the user clicks `AnchoredButton` on the same page, the `ProgressBar` is not notified and continues to display stale progress until next navigation. Is this a correctness gap relative to the ticket outcome ("showing X of N lessons completed")?
5. `ProgressBar` anchor icon at pct=0 — `style={{ left: '0%', transform: 'translateX(-50%) translateY(-50%)' }}` positions the ⚓ icon half-hidden outside the bar's left edge when no lessons are completed. This is a visual regression on the initial load state.
6. `LessonLayout.astro` passes `entry.slug` as `lessonSlug` — the full Astro content collection slug (e.g. `programming-fundamentals/what-is-a-program`). The `ProgressBar` in `LessonLayout.astro` counts lessons via `allLessons.filter((l) => l.data.subject === subject).length` and maps progress keys stored by `AnchoredButton` (using full `entry.slug`). Does `getProgress` return a `completed` count consistent with what `LessonLayout.astro` passes as `totalLessons`? (i.e., are the same slugs both stored and counted across all subject lesson pages?)

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — the localStorage value is a raw `string[]` with no version marker. If a future release changes the schema (e.g. to an object with metadata), existing stored arrays are silently parsed by the new code without migration. Probe whether any version sentinel or migration path exists in the diff.
2. **CLI flag/arg symmetry** — [N/A — this ticket adds no CLI flags or command-line arguments; the change is browser-only React/Astro code]
3. **Error-class breadth in `catch` blocks** — `markComplete`, `getProgress`, and `isComplete` all call `JSON.parse(existing)` without try/catch. A corrupt or tampered localStorage value throws a SyntaxError that propagates to the React component's `useEffect`, potentially crashing the island. Probe whether any error-recovery path exists.
4. **Defensive layering at module boundaries** — `markComplete` and `isComplete` accept arbitrary `lessonSlug` and `subjectSlug` strings without validation. An empty string `subjectSlug` would write to `anchor:progress:`, a valid key that could collide across subjects if a future lesson has no subject set. Probe whether the callers (Astro layout) guarantee non-empty strings.
5. **Cross-file atomicity windows** — `markComplete` does a read-modify-write: `getItem` → mutate array → `setItem`. This is synchronous localStorage but there is no locking. In a multi-tab scenario, two tabs completing the same lesson simultaneously could each read the old array, independently append the slug, and write back — producing a duplicate. Probe whether `markComplete`'s deduplication check (`!slugs.includes`) is sufficient against multi-tab write races.
6. **Test-contract strength** — the three Vitest tests use `vi.stubGlobal` to mock localStorage with a `Map`-backed stub. The `length` property is hardcoded to `0` and the `key()` method always returns `null`. Probe whether this creates a gap between the mock and real localStorage behavior that could hide bugs the tests are supposed to catch.
7. **Doc-vs-code drift in the ticket Rationale** — The ticket Outcome says "triggers an anchor drop/settle CSS animation." The Rationale says "Animation should be a CSS class toggled on click, not a JS animation loop." The implementation uses `animate-bounce` (a Tailwind utility) on the ⚓ span via a `dropping` state flag + 600ms `setTimeout`. Probe whether `animate-bounce` exists in this Tailwind v4 build or is a custom class, and whether it qualifies as "CSS class toggled on click" rather than a JS animation loop.

### Diff context

**`src/lib/progress.ts`** (new, 33 lines): Three exported functions. `markComplete(lessonSlug, subjectSlug)` reads JSON array from localStorage key `anchor:progress:<subjectSlug>`, appends slug if not present, writes back. `getProgress(subjectSlug, totalLessons)` reads and returns `{ completed: slugs.length, total: totalLessons }`. `isComplete(lessonSlug, subjectSlug)` returns boolean from array inclusion check. No error handling for JSON parse failures.

**`src/components/AnchoredButton.tsx`** (new, 46 lines): React island. `useEffect` sets `anchored=true` on mount if lesson is already complete. `handleClick` guards with `if (anchored) return`, calls `markComplete`, then sets `dropping=true` + schedules a 600ms `setTimeout` that sets `dropping=false` and `anchored=true`. Button renders with `disabled={anchored}` and conditional Tailwind classes for green/blue state. Uses `animate-bounce` on the ⚓ span during `dropping`.

**`src/components/ProgressBar.tsx`** (new, 62 lines): React island. `useEffect` reads `getProgress` on mount and updates state. Renders a `div` progress fill bar and overlays ⛓ chain link emojis at evenly-spaced positions, plus a ⚓ anchor icon at `left: ${pct}%` with `translateX(-50%) translateY(-50%)`. When `pct=0`, the anchor is off the left edge.

**`src/layouts/LessonLayout.astro`** (modified): Adds `AnchoredButton client:only="react"` (replacing the static button) and `ProgressBar client:only="react"` (new, after breadcrumb). Passes `entry.slug` as `lessonSlug`, `subject` as `subjectSlug`, and filtered subject lesson count as `totalLessons`.

**`src/pages/[subject]/index.astro`** (modified): Adds `ProgressBar client:only="react"` after the `<h1>` with `subjectSlug={subject}` and `totalLessons={subjectLessons.length}`.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
```
